{
  "name": "proxy-server",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "yarn test:unit && yarn use-case:tests",
    "test:unit": "jest --config=\"jest.config.ts\"",
    "test:unit:watch": "yarn run test:unit -- --watchAll",
    "use-case:tests": "yarn joke-webpage & sleep 2 && yarn e2e-playwright; kill $(lsof -t -i:8000)",
    "joke-webpage": "python3 -m http.server 8000 --directory use-cases/tests/example-page 2> /dev/null",
    "e2e-playwright": "yarn playwright test --config use-cases/tests/playwright.e2e.config.ts"
  },
  "dependencies": {
    "@types/jest": "^29.5.3",
    "axios": "^1.4.0",
    "jest": "^29.6.2",
    "remeda": "^1.24.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@playwright-testing-library/test": "^4.0.1",
    "@playwright/test": "1.28.1",
    "typescript": "^4.8.4"
  }
}
