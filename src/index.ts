/* istanbul ignore file */

import { HttpApiProxyServer as Proxy } from "./proxy-server";
import { HttpApiProxyServerSettings as Settings } from "./proxy-server";

export const HttpApiProxyServer = Proxy;
export type HttpApiProxyServerSettings = Settings;
