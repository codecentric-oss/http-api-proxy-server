name: Publish NPM-Package

on:
  workflow_run:
    workflows: ["Check Package"]
    types:
      - success

jobs:
  publish-package:
    if: ${{ github.event.workflow_run.head_branch == 'conventional-commits-and-auto-releases' }}
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
        run: echo "Should not run if not on FAKE main"
